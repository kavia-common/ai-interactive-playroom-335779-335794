# Backend environment variables (example)
# Do NOT commit real secrets here (none needed for this template backend).

# Express server
PORT=3000
HOST=0.0.0.0

# CORS: set to your frontend origin (scheme + host + port)
# Example for Vite dev server:
FRONTEND_ORIGIN=http://localhost:5173
